openapi: "3.0.0"
info:
  title: Wrong Schema Fixture
  version: "1.0.0"
paths:
  /api/health:
    get:
      operationId: wrong-health-aggregate
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Intentionally wrong response schema for mismatch testing
          content:
            application/json:
              schema:
                type: object
                required:
                  - total_services
                properties:
                  total_services:
                    type: integer
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
